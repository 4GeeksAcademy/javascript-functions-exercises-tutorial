//Define the function called "multi" that expects 2 parameters:
function multi (a, b) {
  return a * b
}

  //don't edit anything below this line
  var returnValue = multi(7,12);
  console.log(returnValue);